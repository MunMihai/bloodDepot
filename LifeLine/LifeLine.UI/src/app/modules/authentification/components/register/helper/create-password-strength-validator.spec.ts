import { CreatePasswordStrengthValidator } from './create-password-strength-validator';

describe('CreatePasswordStrengthValidator', () => {
  it('should create an instance', () => {
    expect(new CreatePasswordStrengthValidator()).toBeTruthy();
  });
});
